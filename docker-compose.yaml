services:
  mindshaala-develop:
    container_name: mindshaala-develop
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8005:8005"
    environment:
      - NODE_ENV=development
    